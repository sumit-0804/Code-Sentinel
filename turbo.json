{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {
      "inputs": ["src/**", "../../eslint.config.mjs"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
